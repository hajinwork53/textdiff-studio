# TextDiff Studio 사용 설명서

본 문서는 비개발자도 따라할 수 있게 단계별로 정리한 사용법입니다. README 의 빠른 시작이 끝난 후 보세요.

## 목차

1. [첫 실행 + UI 둘러보기](#1-첫-실행--ui-둘러보기)
2. [파일 비교 (기본)](#2-파일-비교-기본)
3. [클립보드 비교 (AI 응답 즉시 비교)](#3-클립보드-비교-ai-응답-즉시-비교)
4. [Git 비교 (3 모드)](#4-git-비교-3-모드)
5. [MD 리포트 저장 + VS Code 점프](#5-md-리포트-저장--vs-code-점프)
6. [텍스트 추적 (출력 → 코드 위치)](#6-텍스트-추적-출력--코드-위치)
7. [스냅샷 백업 + 복원](#7-스냅샷-백업--복원)
8. [CLI 모드 (터미널에서 호출)](#8-cli-모드-터미널에서-호출)
9. [단축키 모음](#9-단축키-모음)
10. [자주 묻는 질문](#10-자주-묻는-질문)

---

## 1. 첫 실행 + UI 둘러보기

`npm run dev` 또는 `.exe` 더블 클릭 후 **FilePicker** 화면이 보입니다.

```
┌─ TextDiff Studio ──────────────────────────── _ □ ✕ ┐
│ TextDiff Studio                          ⚙ vscode  │
├────────────────────────────────────────────────────┤
│                                                    │
│ 비교할 파일                          파일 개수: 2  │
│                                                    │
│  ┌────────────────┐    ┌────────────────┐         │
│  │ 슬롯 A         │    │ 슬롯 B         │         │
│  │ [📁 파일 선택]  │    │ [📁 파일 선택]  │         │
│  └────────────────┘    └────────────────┘         │
│                                                    │
│ 빠른 비교                                          │
│ [📋 클립보드 붙여넣기]  [⎇ Git 비교]              │
│ [🔍 텍스트 추적]        [📦 스냅샷 비교]           │
│                                                    │
│                                  [비교 시작 →]    │
└────────────────────────────────────────────────────┘
```

각 버튼:
- **[📁 파일 선택]** — 일반 파일 비교 (가장 단순)
- **[📋 클립보드 붙여넣기]** — AI 응답을 텍스트 그대로 슬롯에 (Ctrl+V 와 동일)
- **[⎇ Git 비교]** — Git 저장소면 활성. 미설치면 회색
- **[🔍 텍스트 추적]** — 비교 시작 안 해도 코드 검색
- **[📦 스냅샷 비교]** — 스냅샷 관리 화면으로
- **[⚙ vscode]** — 외부 에디터 설정 (vscode / cursor / 사용자 정의)

---

## 2. 파일 비교 (기본)

1. 슬롯 A 의 **[📁 파일 선택]** → 파일 다이얼로그 → 파일 선택
2. 슬롯 B 도 동일하게
3. 하단 **[비교 시작 →]** 클릭 (두 슬롯 모두 정상 로드된 후 활성화)
4. **DiffViewer** 화면 진입

### DiffViewer 화면 구성

```
┌─ ← 새 비교 │ ▦ │ 📄 a.txt ↔ 📄 b.txt │  [vscode] [🔍 텍스트추적] [👁 미리보기] [💾 MD 저장] ┐
├──────────────┬───────────────────────────────────────────────────────────────────────────┤
│  좌측 패널    │  📄 A · UTF-8 · 186줄                  │ 📄 B · UTF-8 · 186줄              │
│              │  1   <!DOCTYPE html>                    │  1   <!DOCTYPE html>              │
│  📊 통계      │  2   <html>                              │  2   <html lang="ko">             │
│  변경 5/186  │  3   ...                                 │  3   ...                          │
│  +3 -1 ↻1   │                                          │                                   │
│              │                                          │                                   │
│  📋 변경 목록 │                                          │                                   │
│  ┌────────┐ │                                          │                                   │
│  │ L2-5  ↻ │ │                                          │                                   │
│  │ L42-43+│ │                                          │                                   │
│  │ ...    │ │                                          │                                   │
│  └────────┘ │                                          │                                   │
│              │                                          │                                   │
│  📋 클립보드  │                                          │                                   │
│  패널        │                                          │                                   │
└──────────────┴───────────────────────────────────────────────────────────────────────────┘
```

- 좌측 패널: TOC (변경 목록) + 클립보드 누적 패널. **Ctrl+B** 로 토글
- 가운데: Monaco DiffEditor — 좌측이 A, 우측이 B
- TOC 의 변경 항목 클릭 → 해당 라인으로 점프 + 1초 펄스 하이라이트
- **[← 새 비교]** — 비교 끝났으면 진입 출처로 자연스럽게 복귀 (FilePicker / 스냅샷 관리 등)

---

## 3. 클립보드 비교 (AI 응답 즉시 비교)

가장 자주 쓰는 시나리오. AI 한테 코드 받고 → 임시 파일 안 만들어도 됨.

### FilePicker 에서

1. AI 응답 텍스트 Ctrl+C (보통 이미 클립보드에 있음)
2. FilePicker 에서 **Ctrl+V** → 자동으로 첫 빈 슬롯에 들어감 (파랑 📋 배지)
3. 또는 **[📋 클립보드 붙여넣기]** 버튼 클릭 → 같은 효과
4. 슬롯 채워지면 **[비교 시작 →]**

### DiffViewer 안에서

비교 중인 상태에서도 Ctrl+V → 모달이 뜸:
- **B 슬롯 교체** — 새 응답으로 다시 비교
- **클립보드 패널에만 추가** — 누적 (좌측 패널)

이렇게 AI 가 여러 응답 줘도 모두 누적 + 골라서 비교 가능.

---

## 4. Git 비교 (3 모드)

Git 저장소면 자동 인식. **[⎇ Git 비교]** 버튼 활성화.

### Mode 1 — 현재 변경 사항 (HEAD ↔ Working Dir)

가장 빈번한 시나리오. AI 가 코드 바꾼 직후 "원래랑 뭐가 달라?" 확인.

1. **[⎇ Git 비교]** → 저장소 폴더 선택 (한 번만 하면 다음부터 기억)
2. **"현재 변경 사항"** (기본 선택)
3. 변경된 파일 목록에서 파일 선택 (untracked 신규 파일도 포함)
4. **[비교 시작 →]** → 슬롯 A=HEAD, B=Working

### Mode 2 — 두 커밋 비교

"지난주 a3f2e1d 커밋과 오늘 b8c9d0e 커밋 사이 어떻게 변했지?"

1. **[⎇ Git 비교]** → **"두 커밋 비교"** 라디오
2. From 커밋 / To 커밋 각각 [커밋 선택] 클릭 → 커밋 검색 모달:
   - 검색어 (메시지/해시/작성자 — 퍼지)
   - 브랜치 필터
   - 페이지네이션 (100개씩)
3. 두 커밋 선택되면 변경 파일 자동 로드 → 파일 선택 → 비교

### Mode 3 — 두 브랜치 비교

"feature/auth 와 main 의 차이는?"

1. **"두 브랜치 비교"** 라디오
2. From / To 브랜치 드롭다운 (로컬/원격 그룹, ★ 현재 브랜치)
3. 둘 다 선택되면 자동 diff

### Git 비교 후 [← 새 비교]

비교 화면에서 **[← 새 비교]** → **Git 모달이 자동으로 다시 떠 있는 FilePicker 로 복귀**. 다른 파일 바로 선택 가능.

---

## 5. MD 리포트 저장 + VS Code 점프

비교 결과를 마크다운 파일로 저장. 나중에 다시 열거나 AI 한테 첨부할 때 강력함.

### 저장

1. DiffViewer 우상단 **[💾 MD 저장]** 또는 Ctrl+S
2. 저장 다이얼로그 → 위치 선택
3. 토스트 "📂 폴더 열기" / "📝 에디터로 열기" 액션

### 저장된 MD 안의 내용

```markdown
---
tool: TextDiff Studio
schema_version: 1
generated_at: 2026-05-25T14:32:00+09:00
files:
  a:
    path: D:/proj/old.py
    encoding: UTF-8
    line_count: 186
  b:
    path: D:/proj/new.py
    ...
stats:
  changed_lines: 47
  change_percent: 25.3
---

# Diff Report — old.py ↔ new.py

## 📊 통계
- 변경 라인: 47 / 186 (25.3%)
- ⊕ 추가 5 / ⊖ 삭제 3 / ↻ 수정 4

## 🔍 변경 목록

### 1. ↻ L42-43 (수정 ~2)
- 점프: [A · L42-43](vscode://file/D:/proj/old.py:42) ↔ [B · L42-43](vscode://file/D:/proj/new.py:42)
- A (원본) 전후 컨텍스트:
  ```python
  40    def login(user):
  41        if not user:
  42  ▸     return None
  43  ▸     ...
  44        log("ok")
  ```
- B (변경본) 전후 컨텍스트: ...
- Diff:
  ```diff
  - return None
  + raise AuthError("user required")
  ```
...

## 🤖 AI 활용 가이드
프롬프트 예시 / 점프 링크 안내 / Machine-Readable JSON
```

### 점프 링크 클릭

- **[A · L42-43]** 또는 **[L42:30]** 같은 링크 클릭
- 운영체제가 `vscode://file/...` 프로토콜 인식 → VS Code 열림 → 정확한 라인으로 이동
- VS Code 가 아니라 **Cursor** 쓰면 **[⚙ vscode]** 버튼 → Cursor 로 변경

### 미리보기 ([👁 미리보기])

저장 전 MD 내용 확인. 한국어 / 코드 fence / 점프 링크 모두 정상 렌더링.

---

## 6. 텍스트 추적 (출력 → 코드 위치)

본 도구의 **가장 강력한 차별화 기능**.

### 시나리오

> diff 에서 `"회원가입 완료"` 라는 문자열 봤는데, 이게 코드 어디 출력되는지?

### 사용법

**3가지 진입점** 모두 동일 모달:

1. **DiffViewer 안에서 텍스트 드래그 → 우클릭 → "🔍 이 텍스트의 출력 위치 찾기"**
2. **Ctrl+Shift+F** (어디서든 — FilePicker / DiffViewer / 스냅샷 화면)
3. **FilePicker 의 [🔍 텍스트 추적]** 버튼

### 모달 화면

```
┌─ 🔍 출력 텍스트 → 코드 위치 ──────────────────── ✕ ┐
│ [회원가입 완료                    ] [검색]          │
│ □ Aa 대소문자  □ 단어 whole word  □ .* 정규식       │
│ 파일 [모든 파일 ▾] [직접 입력: *.vue,*.ts]          │
│ 루트 D:\my-project ← 마지막 git repo  [폴더 변경]   │
├────────────────────────────────────────────────────┤
│ 결과 3개 (검색 0.12s)                              │
│                                                    │
│ src/views/SignupSuccess.vue:42                     │
│   <h1>{{ msg }} 회원가입 완료</h1>                 │
│   [→ VS Code] [📋 경로]                           │
│                                                    │
│ src/i18n/ko.json:128                               │
│   "signup_done": "회원가입 완료"                   │
│   [→ VS Code] [📋]                                 │
│                                                    │
│ tests/signup.test.ts:67                            │
│   expect(text).toContain('회원가입')              │
│   [→ VS Code] [📋]                                 │
├────────────────────────────────────────────────────┤
│ [💾 결과를 MD로 저장]                  [닫기 (Esc)] │
└────────────────────────────────────────────────────┘
```

### 옵션

- **Aa 대소문자** / **단어 whole word** / **.* 정규식** — 일반 검색기와 동일
- **파일 글로브** — 프리셋 (Vue/TS/JS, Python, JSON 등) 또는 직접 입력 (`*.vue,*.ts`)
- **루트** — 자동 결정 우선순위: 사용자 선택 > 마지막 git repo > 비교 파일 부모 폴더

### 결과 후 액션

- **[→ VS Code]** — 그 파일/라인으로 점프 (`vscode://`)
- **[📋 경로]** — `src/views/A.vue:42` 형식으로 클립보드 복사
- **[💾 결과를 MD로 저장]** — 검색 결과만 담은 독립 MD 파일. 비교 리포트와 분리 (AI 분석 맥락 보호)

### Skip 파일

OneDrive sync 중이거나 잠긴 파일은 검색 못 함. 결과 메타 옆에 **⚠ N개 파일 skip** 칩 표시 → 클릭하면 파일 목록 펼침. 결과 자체는 그대로 동작.

---

## 7. 스냅샷 백업 + 복원

Git 안 쓰거나 매번 commit 귀찮을 때. "AI 시키기 전 폴더 백업" → AI 결과 망가지면 한 번에 복원.

### 진입

FilePicker → **[📦 스냅샷 비교]** → 스냅샷 관리 화면

### 첫 사용 — 저장 위치 설정

화면 상단 **[⚙ 저장 설정]** → 3 모드:

| 모드 | 위치 | 특징 |
|------|------|------|
| **project** (기본) | `<프로젝트>/snapshots/` | 직관적. ⚠ Git `.gitignore` 추가 필요 / OneDrive 동기화 부담 |
| **appdata** | `%APPDATA%/TextDiff/snapshots/<해시>/` | 깨끗, 프로젝트 안 더럽힘 X |
| **custom** | `<사용자선택폴더>/<해시>/` | 별도 드라이브 등에 |

### 새 스냅샷 만들기

1. **[+ 새 스냅샷]** → 모달:
   - 프로젝트 폴더 (현재 작업 폴더 자동 또는 [폴더 변경])
   - 메모 (예: "AI 리팩토링 전")
   - 고급 설정: 압축 강제 / 핀 고정 (자동 삭제 제외)
2. **[📦 생성 시작]** → zip 생성 (또는 10K 파일/50MB 초과 시 fs.cp 폴더 복사 자동 전환)
3. 토스트 "📦 스냅샷 만들어짐"
4. 관리 화면 카드에 추가됨

### 제외 패턴 (자동)

다음은 자동 제외 (백업 안 됨):
- `.git/`, `node_modules/`, `dist/`, `build/`, `out/`, `__pycache__/`, `target/`
- `*.log`, `*.tmp`, `.DS_Store`, `Thumbs.db`
- `snapshots/`, `.textdiff-snapshots/` (자기 자신)

사용자 `.textdiffignore` 파일도 자동 인식 (gitignore 문법).

### 두 스냅샷 비교

1. 카드의 **[📊 비교 시작]** → 다른 스냅샷 선택 모달 (또는 "현재 폴더 상태와 비교")
2. 변경된 파일 목록 → 파일 선택 → DiffViewer 진입
3. 비교 화면에서 [← 새 비교] → 스냅샷 관리 화면으로 복귀

### 복원 (⚠ 위험)

카드의 **[⤴ 복원]** → 빨강 모달. 3중 안전장치:

1. **자동 백업** — 복원 직전 현재 상태를 `AUTO_BEFORE_RESTORE_<ts>` 로 저장 (핀 고정)
2. **자동 롤백** — 복원 도중 에러 발생 시 자동 백업으로 즉시 되돌림
3. **디스크 공간 사전 검사** — 필요 × 1.2 마진

영향 분석 박스:
- **⊕ N 신규** — 복원 시 새로 추가될 파일 (스냅샷에만 존재)
- **⟳ N 실제 수정** — 내용 hash 비교 결과 다른 파일 (덮어쓰기)
- **✓ N 동일** — 내용 같음 (안 건드림 — 옵션 OFF 시 표시 안 함)
- **⊖ N 삭제** — 스냅샷에 없는 파일 (지울 것)

각 박스 클릭 → 파일 100개 미리보기.

**확인** — 스냅샷 ID 정확히 타이핑 (실시간 ✓/❌ 표시) → **[🔴 복원 진행]** 활성.

### 핀 고정

카드의 **[📌 핀 고정]** → 보관 cap (기본 20개) 초과 시 자동 삭제 대상에서 제외.

---

## 8. CLI 모드 (터미널에서 호출)

터미널에서 한 줄로 비교 시작. 자동화 / AI agent 가 호출 / VS Code task 등.

### 사용법

`npm run build:cli` 한 번 빌드 후:

```powershell
# 두 파일 비교
node dist-cli/textdiff.js README.md CHANGELOG.md

# Git 현재 변경 사항 (특정 파일)
node dist-cli/textdiff.js --git-working src/App.vue

# Git 모달 자동 오픈 (파일 선택 안 한 상태)
node dist-cli/textdiff.js --git-working

# 두 커밋 비교
node dist-cli/textdiff.js --git-commits HEAD~3 HEAD src/App.vue

# 두 브랜치 비교
node dist-cli/textdiff.js --git-branches main feature/login src/auth.ts

# 도움말
node dist-cli/textdiff.js --help

# 버전
node dist-cli/textdiff.js --version
```

### 두 번째 호출

앱이 이미 떠있을 때 또 호출하면 → **새 창 X**, 기존 창에 인자 전달 + 포커스 (single-instance lock).

### 패키지된 .exe 의 CLI

현재 v0.1 의 패키지된 `.exe` 는 CLI 진입을 dev 모드만 지원. `textdiff` 명령어를 PATH 에 등록하는 packaging 은 v1.1+ 예정.

---

## 9. 단축키 모음

### 전역 (어디서든)

- **Ctrl+Shift+F** — 텍스트 추적 모달 (선택 텍스트 자동 입력)

### FilePicker

- **Ctrl+V** — 클립보드 → 첫 빈 슬롯

### DiffViewer

- **Ctrl+S** — MD 리포트 저장
- **Ctrl+B** — 좌측 패널 토글 (TOC + 클립보드)
- **Ctrl+Shift+F** — 텍스트 추적 (Monaco 선택 텍스트 우선)
- **Ctrl+V** — 클립보드 → B 슬롯 교체 (확인 모달)
- **Esc** — 모달 닫기

### Monaco 우클릭

- **"🔍 이 텍스트의 출력 위치 찾기"** — 선택 시 활성

---

## 10. 자주 묻는 질문

### Q. 한글 인코딩 깨질 때
**A.** 자동 감지 (jschardet + iconv-lite) — UTF-8 / CP949 / EUC-KR 정확히 잡음. 그래도 깨지면 슬롯의 [⚙ 인코딩] 메뉴 → 강제 변경.

### Q. 비교 후 [← 새 비교] 가 메인으로 갈 때
**A.** v0.1 hotfix 적용됨. Git 비교에서 왔으면 Git 모달이 자동 다시 떠있는 FilePicker, 스냅샷 비교에서 왔으면 스냅샷 관리 화면, 그 외엔 FilePicker.

### Q. `.exe` 더블 클릭해도 안 뜰 때
**A.** PowerShell 의 `ELECTRON_RUN_AS_NODE=1` 환경변수가 set 되어 있으면 Electron 이 일반 Node 처럼 동작 → silent exit. 다음 명령 후 다시 시도:
```powershell
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue
```

### Q. SmartScreen 경고
**A.** 코드 사이닝 없음 (1인 도구). 처음 1회만:
- 파란 창 → **[자세한 정보]** 또는 **[추가 정보]** 클릭 → **[실행]** 버튼 클릭
- 또는 영구 해결: 파일 우클릭 → 속성 → 일반 탭 → **[차단 해제]** 체크

### Q. Git 비교 모달의 [📂 위치] 버튼은?
**A.** Git 비교가 아니라 스냅샷 관리 카드의 버튼. 해당 zip 파일을 탐색기에서 열기.

### Q. 스냅샷 자동 모드는?
**A.** v0.1 은 수동만. 자동 trigger (감시 폴더 + 변경 감지 / 시간 기반 / idle 감지) 는 dogfooding 피드백 받고 v1.1+ 에서 결정 예정.

### Q. 코드에 텔레메트리 / 외부 통신 있나?
**A.** 없음. 100% 로컬. 외부 통신은:
- VS Code 점프 (`vscode://`) — 로컬 OS 프로토콜
- Git 명령 (simple-git) — 로컬 git CLI 호출

### Q. Mac / Linux 지원
**A.** v0.1 은 Windows 만. ripgrep 바이너리도 Windows x64 만 묶음. v1.1+ 에서 cross-platform 검토.

---

## 도움 요청

기능 제안 / 버그 / README 보강 → GitHub Issues 환영.
